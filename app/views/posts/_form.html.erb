

<div class = "container-fluid">
  <%= form_for(@post) do |f| %>
    <% if @post.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

        <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div style ="text-align:center; width:30%;display:inline-block;color:black" >
      <div class="field">
        <%= f.label :title %><br>
        <%= f.text_field :title, class: "form-control",maxlength: 50  %>
      </div>
      <div class="field">
        <%= f.label :image %><br />
        <%= f.file_field :image, class: "form-control" %>
      </div>
      <div class="field">
        <%= f.label :description %><br>
        <%= f.text_area :description, class: "form-control",maxlength: 1000 %>
      </div>
      <div class="field">
        <%= f.label :price %>
        $<%= f.text_field :price, class: "form-control", maxlength:5 %>
      </div>
      <div class="field">
        <%= f.label :location %><br>
        <%= f.text_field :location, class: "form-control" %>
      </div>
      <br>
      <div class="actions">
        <%= f.submit %>
      </div>
    </div>

  <% end %>
</div>
